# macOS
.DS_Store
**/.DS_Store

# Dev artefacts
Screenshots/
*.log
.env
.env.local

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Vercel
.vercel

# Claude Code session data
.claude/


# >100MB files — GitHub limit. Keep locally, never commit.
images/Project03_Ambiance_ChristmasCampaign/AMBIANCE FINAL HERO.MOV


# Large video originals — keep locally, never commit. Per-project
# videos are served from Cloudinary (the project pages already point
# at player.cloudinary.com), so these are off-site referenced.
*.MOV
*.mov
*.MP4
images/**/*.mov
images/**/*.MOV
